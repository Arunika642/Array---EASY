#include<bits/stdc++.h>
int getSingleElement(vector<int> &arr){
	// Write your code here.
	int XOR = 0;
	for(int i=0;i<arr.size();i++)
	{
		XOR = XOR^arr[i];
	}
	return XOR;
}
//TC = O(N)
//SC = O(1)
